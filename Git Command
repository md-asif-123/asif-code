Git Command
===========
[credential]
  helper = osxkeychain
  useHttpPath = true
  
git --version
git config --global user.name "your name"
git config --global user.email "your email"
git config --global --edit (to save for escape :wq )
git init
git status
git add filename(Staging a file)
git commit -m "initial commit"
git log
git add .(Satging all file)
git checkout 665e6fee3f10f42f20e14b5bec890ed61c7cc015
git checkout master(To enter into a new branch)
git branch
(If you are trying to access a 'master' branch that exists only on a remote repository, you need to fetch it first. Run git fetch origin)
git branch dev(To create a new branch)
git checkout -b asif/demo(To checkout into a new branch)
git merge asif/demo
touch .gitignore(To create .gitignore file)
git remote add origin https://github.com/mdasif-123/lets-learn-git.git
git remote -v
git branch -M master
git push -u origin dev
git push
git clone https://github.com/md-asif-123/First-Contribution.git



